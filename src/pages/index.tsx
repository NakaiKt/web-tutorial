import { Typography } from "@mui/material";

const pages = () => {
  return (
    <div>
      <Typography>
        これはCSS, MUI, React, ReactHookFormのチュートリアルです
      </Typography>
    </div>
  );
};
export default pages;
