import AutoLayout from "@/components/templates/Base/AutoLayout";
import { BaseTemplate } from "@/features/Template";

const Pages = () => {
  return (
    <BaseTemplate title="オートレイアウト（FlexとGrid）">
      <AutoLayout />
    </BaseTemplate>
  );
};

export default Pages;
