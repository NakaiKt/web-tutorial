import Base from "@/components/templates/Base";
import { BaseTemplate } from "@/features/Template";

const Pages = () => {
  return (
    <BaseTemplate title="基本">
      <Base />
    </BaseTemplate>
  );
};

export default Pages;
